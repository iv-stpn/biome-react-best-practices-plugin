---
"biome-react-best-practices-plugin": patch
---

Bump devDependencies (`@biomejs/biome` to `>=2.5.4`, `biome-one-liner-plugin` to `1.4.0`, `typescript` to `^7`) and drop a stale comment in `react.grit` referencing the removed memoization-preservation rules. No user-facing rule changes.
